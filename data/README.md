# Data directory

CIFAR-10 is downloaded here automatically when you run the notebook.

Files after download:
- `cifar-10-python.tar.gz` (~163 MB)
- `cifar-10-batches-py/` (extracted, ~340 MB total)

Not tracked in git. Run the notebook on any machine to reproduce.
